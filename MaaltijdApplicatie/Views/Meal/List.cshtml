@model IEnumerable<MealDate>

<!-- Loop the meal data models -->
@foreach (var m in Model) {

    if (m.Meal != null) {
        @Html.Partial("MealDateSummary", m)
    } else {
        @Html.Partial("MealDateSummaryNoMeal", m)
    }

}